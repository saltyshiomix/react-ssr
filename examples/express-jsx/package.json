{
  "private": true,
  "name": "express-jsx",
  "license": "MIT",
  "scripts": {
    "build:development": "react-ssr-build server.js --mode development",
    "build:production": "react-ssr-build server.js --mode production",
    "start": "npm run build:development && node server.js",
    "start:prod": "npm run build:production && cross-env NODE_ENV=production node server.js"
  },
  "dependencies": {
    "@react-ssr/express": "latest",
    "@react-ssr/express-engine-jsx": "latest",
    "express": "^4.17.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-helmet": "^5.2.1"
  },
  "devDependencies": {
    "cross-env": "^5.2.0"
  }
}
