export = index;
declare class index {
  static createStats(options: any): any;
  static populateFilesystem(options: any): void;
  static statsDate(inputDate: any): any;
  constructor(options: any);
  options: any;
  apply(compiler: any): void;
}
